# AGENTS.md

## 1. プロジェクト定義

このプロジェクトは、AIニュース閲覧アプリではない。  
海外AIニュース・論文・OSS・SNS情報を、提案ネタ・PoC候補・設計書候補・営業活用ネタ・SNS投稿ネタに変換するための `AI Proposal Intelligence OS` である。

目的は、毎朝3分で「今日使えるAI活用アイデア」を発見し、提案・PoC・設計・共有アクションへ進める状態を作ることにある。  
単なる要約ではなく、実務活用・社内提案・営業活用・SNS活用まで落とし込むことを重視する。

## 2. 開発時の最重要原則

- 既存Mock体験を壊さない
- `pipeline / collectors / analyzer / server / cli` の責務をむやみに崩さない
- UI改善は歓迎するが、複雑化させない
- ニュース一覧ではなく、Brief中心の体験を守る
- Notion / Slack / 有料LLM API は段階的に追加する
- 外部連携やAPIキーは `.env` で管理し、Gitに含めない
- 変更後は必ずテスト・構文チェック・Mock実行を確認する
- 仕様変更をした場合は README または docs も更新する
- 破壊的変更を避ける
- 大きな改修は、目的・影響範囲・変更ファイルを明示する

## 3. UI/UX方針

### 目指すUI

- ChatGPT / Notion / Slack / LINE / Codex のようにシンプルで直感的
- 白ベース
- 余白多め
- カード型
- 情報量は多いが、見た目は軽い
- 重要なものから上に表示
- 毎朝3分で判断できる
- 管理画面っぽくしすぎない
- ニュースサイト風にしない
- 研究論文DB風にしない

### 画面構成の基本

- 上部: 「今日のAI提案ネタ」ヒーロー
- サマリー: 本日の新着件数 / 重要ネタ件数 / PoC候補件数 / 設計書候補件数
- フィルター: すべて / 重要 / ニュース / 論文 / OSS / SNS / PoC候補
- メイン: Briefカード一覧
- 下段: 次にやること / 設計書候補
- 最下段: 収集ログ
- 既存の `Timeline / Ideas / Design Docs / Sources` も同じトーンで整理する

### UIでやらないこと

- フィルターを増やしすぎない
- ボタンを増やしすぎない
- ダッシュボードを複雑にしすぎない
- グラフを安易に増やさない
- 派手なグラデーションや過剰演出に寄せない
- 情報を1画面に詰め込みすぎない
- 専門用語だけで表示しない

## 4. Brief設計ルール

Briefは「ニュース要約」ではなく「提案変換」である。

### Briefに含めるべき項目

- `title`
- `oneLineSummary`
- `whatHappened`
- `whyImportant`
- `businessImpact`
- `pocIdea`
- `designDocCandidate`
- `salesUseCase`
- `snsPostIdea`
- `targetAudience`
- `priority`
- `difficulty`
- `threeDayAction`
- `risks`
- `sourceUrl`

### 文字量ルール

- 一言要約は80文字以内を目安にする
- 「何が起きたか」は3行以内
- 「なぜ重要か」は3行以内
- PoC案は3項目以内
- SNS投稿ネタは1〜2案まで
- 一覧カードでは情報を詰め込みすぎず、詳細で深掘りできる構成にする

### Briefカードに表示する基本情報

- 重要度 `S / A / B / C`
- タイトル
- 一言要約
- 使えそうな部署・用途
- 3日以内に試せること
- 情報源
- なぜ上位表示されているかの簡単な理由

## 5. 優先度・スコアリングルール

### 優先度定義

- `S`: すぐに社内提案・PoCに使える。業務効率化や売上貢献につながる可能性が高く、3日以内に小さく試せる。経営層や事業部に共有する価値がある。
- `A`: 近いうちに活用可能性がある。特定部門に刺さる。設計書候補や調査テーマとして有用。PoC化には少し整理が必要。
- `B`: 参考情報として価値がある。今すぐ使うには遠いが、技術理解・市場理解には役立つ。
- `C`: 今の事業・業務には関係が薄い。話題性はあるが提案化しにくい。保存優先度は低い。

### 評価項目

- `Business Impact`: 事業影響
- `PoC Feasibility`: 試しやすさ
- `Novelty`: 新規性
- `Urgency`: 今見るべき度
- `Relevance`: 自社業務との関連度
- `Shareability`: 共有・SNS化しやすさ

### Briefカードの並び順

- 単純なランク順ではなく「今日やる価値順」
- `S / A` を優先
- PoC候補を優先
- 設計書候補を優先
- 社内活用しやすいものを優先
- 3日以内に試せるものを優先
- 情報が新しいものを優先
- 同じようなネタが連続しすぎないようにする

### 実装方針

- 初期はルールベースでよい
- スコアリング関数は分離して管理しやすくする
- 将来LLMスコアリングに置き換えやすくする

## 6. 情報ソース定義

### RSS / 海外AIニュース

- 事業・市場・プロダクト動向を把握する
- 社内提案・営業提案のネタに使う

### arXiv

- 技術トレンド・研究テーマを把握する
- PoC候補・設計書候補の素材に使う

### GitHub Trending

- 実装可能なOSSや開発トレンドを把握する
- PoC実装・技術検証の候補に使う

### Hacker News

- 海外エンジニア・スタートアップ界隈の反応を見る
- 実務者目線の温度感を確認する

### SNS / Reddit / X など

- 拡散性・話題性・一般層の反応を見る
- SNS投稿ネタや注目テーマの検知に使う
- 初期版では無理に実装せず、将来拡張扱いでよい

## 7. 収集ルール

- 収集は原則1日1回を想定する
- 取得対象は直近24〜72時間以内の情報を優先する
- URLまたはtitleが同一のものは重複扱いにする
- 同じテーマが複数ソースで出た場合は統合候補として扱う
- ソース取得に失敗してもpipeline全体は止めない
- 失敗したソースは `status: failed` として記録する
- 取得件数が0件でも必ずしもエラー扱いにしない
- Mock mode と real mode を明確に分ける
- ネットワーク失敗時はMock fallbackまたは空配列で安全に継続する
- 取得結果は共通フォーマットに正規化する

### 正規化フォーマット例

- `id`
- `source`
- `title`
- `url`
- `summary`
- `publishedAt`
- `tags`
- `raw`

## 8. 保存・差分管理ルール

### 保存対象

- `rawItems`
- `normalizedItems`
- `briefs`
- `opportunityBriefs`
- `sourceLogs`
- `userActions`

### 初期保存先

- `data/` 配下のJSON
- 将来的にSQLite / Notion DBに移行しやすい構造にする

### 保存方針

- 日付単位
- source単位
- item単位
- 同じURLまたは同じtitleは重複候補として扱う

### 差分ステータス

- `new`: 今回初めて取得した情報
- `existing`: 過去に取得済み
- `updated`: 同一URLだが内容が変化
- `duplicated`: 重複候補
- `archived`: 優先度が低く保存のみ

### Timelineで扱う状態

- 今日
- 昨日
- 今週
- 保存済みBrief
- 新規ネタ
- 更新されたネタ
- 提案化済み
- Slack共有済み
- Notion保存済み

## 9. 配信ルール

### Slack通知

- 原則S/Aのみ通知
- 1日最大5件を目安にする
- 全件通知しない
- 通知文は短くする
- 元記事URLを必ず含める
- PoC候補または社内提案候補を優先する
- Webhook URLやTokenは `.env` 管理にする
- 通知失敗時もpipeline全体を止めない

### Notion保存

- 全Briefを保存する必要はない
- S/Aは保存候補
- Bは週次まとめ候補
- Cは原則保存しない、またはrawのみ保存
- Notion連携は段階実装とし、初期版ではUI導線のみでもよい

### 現段階の方針

- Slack / Notion の実連携はまだ不要
- まずはUI上の導線、コピー、状態表示まででよい

## 10. 実装時の制約

- 有料API前提にしない
- OpenAI / Claude / Gemini API は将来拡張扱い
- 外部APIキーやWebhook URLをコードに直書きしない
- `.env.example` は作ってよい
- Mockデータを削除しない
- 既存テストを壊さない
- UI変更だけでpipelineを壊さない
- pipeline変更時はCLI Mock実行まで確認する
- ブラウザ確認できない環境では、理由を記録し、構文チェックとテストで代替確認する

## 11. テスト・確認ルール

### 変更後に基本確認するコマンド

- `node --test`
- `node --check public/app.js`
- `node src/cli.mjs`

### 必要に応じて確認

- `git status`
- `git diff`
- ローカルサーバー起動
- ブラウザ表示確認

### 完了報告に含めるもの

- 変更内容の要約
- 変更ファイル一覧
- テスト結果
- ローカル確認できなかった場合の理由
- 影響範囲
- 次にやるべきこと

## 12. README / docs 更新ルール

- 仕様やルールを追加した場合は README または docs を更新する
- UI思想が変わる場合は `docs/ui-ux-principles.md` を更新する
- Brief構造が変わる場合は `docs/brief-schema.md` を更新する
- スコアリングが変わる場合は `docs/scoring-rules.md` を更新する
- 保存仕様が変わる場合は `docs/storage-policy.md` を更新する
- 配信仕様が変わる場合は `docs/delivery-policy.md` を更新する
- `docs/` がまだ存在しない場合は、将来作成予定として扱う

## 13. 今後の開発ロードマップ

### Phase 1: 土台完成

- Mock UI
- Pipeline
- CLI
- Server
- Test

### Phase 2: 実運用化

- 実データ取得
- 収集ログ
- 保存
- 差分管理
- 重複排除
- 失敗時fallback

### Phase 3: 提案OS化

- Brief品質改善
- PoC案生成
- 設計書候補生成
- 営業ネタ化
- SNSネタ化
- 優先度スコアリング改善

### Phase 4: 配信

- Slack通知
- Notion保存
- 週次レポート
- 月次まとめ

### Phase 5: AI高度化

- OpenAI API
- Claude API
- Gemini API
- LLMスコアリング
- 社内RAG連携

## 14. Codexへの作業姿勢

- 迷った場合は「ニュースアプリ」ではなく「提案OS」に寄せる
- 迷った場合は機能追加よりシンプルさを優先する
- 迷った場合はMock体験を壊さない
- 迷った場合はREADME / docs / AGENTS.mdにルールを残す
- 迷った場合はテストが通る状態を優先する
- 大きな判断が必要な場合は、実装前に方針を短く整理する
- 不要に新しい依存パッケージを増やさない
- 将来拡張しやすいが、今は過剰実装しない

## 補足

- `src/pipeline.mjs` と `src/server.mjs` の構成は素直であり、むやみに壊さない
- `src/collectors.mjs` の RSS / arXiv / GitHub Trending / Hacker News の入口は維持方針
- `src/local-analyzer.mjs` は現状ルールベース中心で、今後のBrief品質改善対象
- ローカルブラウザ確認は環境により `127.0.0.1:3305` が `EPERM` となる場合がある
